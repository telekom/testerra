export class Threads {
  message: string;

  constructor() {
    this.message = 'As can be clearly seen, the tab bar from MDC does not behave correctly as it doesn\'t load the animations correctly when switching between tabs ';
  }
}
