dependencies {
    compile core
    compile selenium
    compile datatypes
    compile image_processing
    //compile group: 'com.google.inject', name: 'guice', version: '4.0'
    //compile 'com.google.inject:guice-parent:4.2.3-SNAPSHOT'
}

test {
    exclude '**'
}
