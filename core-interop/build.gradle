dependencies {
    compile apache_commons
    compile slf4j
    compile zip4j
    compile testng
    compile colt
    compile reflections
    compile "org.mongodb:bson:3.7.0"
}

test {
    exclude '**'
}